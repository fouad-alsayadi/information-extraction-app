# Local Development Secrets Template
# Copy this file to secrets.yaml and fill in your local secrets
# secrets.yaml is git-ignored for security

database:
  password: "your_local_password_here"

# Optional: Override upload path for local development
# upload:
#   base_path: "/path/to/local/uploads"
